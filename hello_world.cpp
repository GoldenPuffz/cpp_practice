#include <iostream> //header file library that let's us use input and outputs
int main(){//function where everything inside will be executed
    std::cout << "Hello World"; //The using namespace std line can be omitted and replaced with the std keyword, followed by the :: operator for some objects:
    return 0;//ends main function
}